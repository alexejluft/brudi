# Agent Read Bootchain Proof

**Date:** 2026-02-24
**Method:** Static analysis of `use.sh` — no execution performed.

---

## 1. Files Present in Installation (read directly by agents)

These files live in `~/Brudi/` and agents read them directly. They are NOT copied by `use.sh`.

| File | Path in Install | Purpose | Evidence |
|------|----------------|---------|----------|
| `AGENTS.md` | `~/Brudi/AGENTS.md` | Global agent instructions | ✅ 5872 bytes |
| `CLAUDE.md` | `~/Brudi/CLAUDE.md` | Global Claude Code context | ✅ 24057 bytes |
| `skills/*/SKILL.md` | `~/Brudi/skills/<name>/SKILL.md` | Domain-specific knowledge | ✅ 74 skill dirs |

---

## 2. Files Generated by `use.sh` into Projects

Static analysis of `use.sh` — line references to the actual install file:

### AGENTS.md → Project Root

**Source:** Inline heredoc in `use.sh` (lines 86–122)
**Condition:** Created if file missing OR file exists but doesn't contain "brudi" (lines 78–83)
**Content:** References `~/Brudi/AGENTS.md`, `~/Brudi/CLAUDE.md`, `~/Brudi/skills/`, Tier-1 orchestration rules, gate commands

### CLAUDE.md → Project Root

**Source:** `~/Brudi/templates/CLAUDE.md` (line 137–141)
**Condition:** Created if file missing OR file exists but doesn't contain "brudi" (lines 129–133)
**Transform:** `sed` substitution replaces `[Dein Projektname]` and `[Projektname]` with actual project name
**Fallback:** If template not found, creates minimal version inline (lines 143–151)

### TASK.md → Project Root

**Source:** `~/Brudi/templates/TASK.md` (lines 158–163)
**Condition:** Created only if file doesn't exist
**Transform:** `sed` substitution replaces `[Projektname]` with actual project name

### PROJECT_STATUS.md → Project Root

**Source:** `~/Brudi/templates/PROJECT_STATUS.md` (lines 165–171)
**Condition:** Created only if file doesn't exist
**Transform:** `sed` substitution replaces `[Projektname]` and `[Datum]` with project name and current date

### Problems_and_Effectivity.md → Project Root

**Source:** Inline heredoc in `use.sh` (lines 191–211)
**Condition:** Created only if file doesn't exist
**Content:** Empty template with Phase 0 stub entry

### .brudi/state.json → .brudi/

**Source:** `~/Brudi/orchestration/state.init.json` (lines 59–67)
**Condition:** Created only if file doesn't exist
**Transform:** `jq` (preferred) or `sed` (fallback) sets `project` and `brudi_version` fields
**Initial Values:** `mode: "BUILD"`, `phase: 0`, `mode_set_by: "task_md"`

### eslint.config.brudi.js → Project Root

**Source:** `~/Brudi/templates/eslint.config.brudi.js` (lines 219–226)
**Condition:** Created only if file doesn't exist
**Transform:** Direct copy (no substitution)

### src/primitives/* → src/primitives/

**Source:** `~/Brudi/templates/primitives/` (lines 175–186)
**Files:** `layout.tsx`, `tokens.ts`, `use-scroll-reveal.ts`, `use-stagger-entrance.ts`
**Condition:** Each file copied only if target doesn't exist
**Transform:** Direct copy (no substitution)

### .git/hooks/pre-commit → .git/hooks/

**Source:** `~/Brudi/orchestration/pre-commit` (lines 230–243)
**Condition:** Only if `.git/` directory exists
**Transform:** Direct copy + `chmod +x`

### .gitignore Amendment

**Source:** Inline in `use.sh` (lines 248–254)
**Condition:** Only if `.gitignore` exists and doesn't already contain `.brudi/state.json`
**Content:** Appends commented-out line for `.brudi/state.json`

---

## 3. Source File Existence Verification

All source files that `use.sh` reads from the installation:

| Source File | Exists in Install? | Size |
|------------|-------------------|------|
| `templates/CLAUDE.md` | ✅ | 11580B |
| `templates/TASK.md` | ✅ | 6116B |
| `templates/PROJECT_STATUS.md` | ✅ | 9166B |
| `templates/eslint.config.brudi.js` | ✅ | 3366B |
| `templates/primitives/layout.tsx` | ✅ | 4848B |
| `templates/primitives/tokens.ts` | ✅ | 8593B |
| `templates/primitives/use-scroll-reveal.ts` | ✅ | 6993B |
| `templates/primitives/use-stagger-entrance.ts` | ✅ | 8543B |
| `orchestration/state.init.json` | ✅ | 336B |
| `orchestration/pre-commit` | ✅ | 7733B |
| `VERSION` | ✅ | 6B (content: `3.4.0`) |

**Score: 11/11 ✅ — All source files present**

---

## 4. Agent Boot Sequence (Static Proof)

When an agent starts working in a Brudi project, the expected read order is:

```
1. Read ~/Brudi/AGENTS.md          ← PRESENT in install ✅
2. Read ~/Brudi/CLAUDE.md          ← PRESENT in install ✅
3. Read <project>/CLAUDE.md        ← GENERATED by use.sh from templates/CLAUDE.md ✅
4. Read <project>/.brudi/state.json ← GENERATED by use.sh from orchestration/state.init.json ✅
5. Run: brudi-gate.sh pre-slice    ← PRESENT in install at orchestration/brudi-gate.sh ✅
6. Load skill: ~/Brudi/skills/<x>/SKILL.md ← 74 skills PRESENT ✅
```

**All boot chain dependencies satisfied. ✅**

---

## 5. What Is NOT Copied (Stays in Install Only)

These components are never copied into projects — agents access them directly from `~/Brudi/`:

- `orchestration/brudi-gate.sh` (and sub-gates)
- `orchestration/ast-engine/*`
- `orchestration/outcome-engine/*`
- `skills/*/SKILL.md`
- `AGENTS.md` (global)
- `CLAUDE.md` (global)
