---
import Layout from '../layouts/Layout.astro';
---

<Layout title="brudi.dev — Accessibility">
  <main class="max-w-4xl mx-auto px-6 py-16">
    
    <header class="mb-16">
      <a href="/" class="text-zinc-500 hover:text-zinc-300 text-sm mb-4 inline-block">← Zurück</a>
      <h1 class="text-4xl font-bold mb-2 page-title">
        Accessib<span class="text-blue-500">ility</span>
      </h1>
      <p class="text-zinc-400">
        Web für alle. Keine Ausreden.
      </p>
    </header>

    <!-- Why it matters -->
    <section class="mb-16 p-6 bg-blue-500/10 border border-blue-500/30 rounded-xl">
      <h2 class="text-blue-400 font-medium mb-3">💡 Warum Accessibility?</h2>
      <ul class="space-y-2 text-zinc-300">
        <li>• <strong>15% der Weltbevölkerung</strong> hat eine Behinderung</li>
        <li>• <strong>SEO Boost</strong> — Google liebt accessible Websites</li>
        <li>• <strong>Bessere UX für alle</strong> — Keyboard-User, Mobile, etc.</li>
        <li>• <strong>Rechtlich relevant</strong> — WCAG 2.1 AA oft Pflicht</li>
      </ul>
    </section>

    <!-- Quick Wins -->
    <section class="mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-zinc-200">
        Quick Wins
        <span class="text-sm font-normal text-zinc-500 ml-2">80% Impact mit 20% Aufwand</span>
      </h2>

      <!-- Semantic HTML -->
      <article class="mb-8 bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <h3 class="text-lg font-medium mb-4 flex items-center gap-2">
          <span class="text-2xl">🏗️</span> Semantic HTML
        </h3>
        
        <div class="grid md:grid-cols-2 gap-4 mb-4">
          <div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4">
            <h4 class="text-red-400 font-mono text-sm mb-2">❌ Schlecht</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;div class="header"&gt;
&lt;div class="nav"&gt;
&lt;div onclick="..."&gt;Click&lt;/div&gt;</code></pre>
          </div>
          <div class="bg-green-500/10 border border-green-500/30 rounded-lg p-4">
            <h4 class="text-green-400 font-mono text-sm mb-2">✅ Gut</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;header&gt;
&lt;nav&gt;
&lt;button&gt;Click&lt;/button&gt;</code></pre>
          </div>
        </div>
        
        <p class="text-zinc-400 text-sm">
          Screenreader verstehen <code>&lt;button&gt;</code>, aber nicht <code>&lt;div onclick&gt;</code>
        </p>
      </article>

      <!-- Alt Text -->
      <article class="mb-8 bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <h3 class="text-lg font-medium mb-4 flex items-center gap-2">
          <span class="text-2xl">🖼️</span> Alt Text für Bilder
        </h3>
        
        <div class="space-y-3 text-sm">
          <div class="flex gap-3">
            <span class="text-green-400">✅</span>
            <code class="text-zinc-300">alt="Golden Retriever spielt im Park"</code>
          </div>
          <div class="flex gap-3">
            <span class="text-red-400">❌</span>
            <code class="text-zinc-300">alt="Bild" / alt="foto.jpg" / alt=""</code>
          </div>
          <div class="flex gap-3">
            <span class="text-yellow-400">⚠️</span>
            <code class="text-zinc-300">alt=""</code>
            <span class="text-zinc-400">— OK für dekorative Bilder</span>
          </div>
        </div>
      </article>

      <!-- Color Contrast -->
      <article class="mb-8 bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <h3 class="text-lg font-medium mb-4 flex items-center gap-2">
          <span class="text-2xl">🎨</span> Farbkontrast
        </h3>
        
        <p class="text-zinc-400 text-sm mb-4">
          WCAG AA: mindestens <strong>4.5:1</strong> für normalen Text, <strong>3:1</strong> für großen Text.
        </p>
        
        <div class="grid md:grid-cols-2 gap-4">
          <div class="p-4 rounded-lg" style="background: #1a1a1a; color: #666;">
            <span class="text-red-400 text-xs">❌ 2.4:1</span>
            <p>Schwer lesbar</p>
          </div>
          <div class="p-4 rounded-lg" style="background: #1a1a1a; color: #a1a1aa;">
            <span class="text-green-400 text-xs">✅ 7.1:1</span>
            <p>Gut lesbar</p>
          </div>
        </div>
        
        <p class="text-zinc-500 text-sm mt-4">
          Tool: <a href="https://webaim.org/resources/contrastchecker/" target="_blank" class="text-blue-400 hover:text-blue-300">WebAIM Contrast Checker</a>
        </p>
      </article>

      <!-- Focus States -->
      <article class="mb-8 bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <h3 class="text-lg font-medium mb-4 flex items-center gap-2">
          <span class="text-2xl">🎯</span> Focus States
        </h3>
        
        <p class="text-zinc-400 text-sm mb-4">
          Keyboard-User müssen sehen wo sie sind. <strong>Nie <code>outline: none</code> ohne Alternative!</strong>
        </p>
        
        <pre class="text-sm overflow-x-auto mb-4"><code class="text-zinc-300">/* Gut: Custom Focus Style */
button:focus-visible {'{'}
  outline: 2px solid #3b82f6;
  outline-offset: 2px;
{'}'}

/* Schlecht: Focus komplett entfernen */
button:focus {'{'}
  outline: none; /* ❌ NIE! */
{'}'}</code></pre>

        <div class="flex gap-4 mt-4">
          <button class="px-4 py-2 bg-blue-500 rounded focus:outline-none">Ohne Focus ❌</button>
          <button class="px-4 py-2 bg-blue-500 rounded focus-visible:outline focus-visible:outline-2 focus-visible:outline-blue-300 focus-visible:outline-offset-2">Mit Focus ✅</button>
        </div>
        <p class="text-zinc-500 text-sm mt-2">Drück Tab um den Unterschied zu sehen</p>
      </article>

      <!-- Labels -->
      <article class="mb-8 bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <h3 class="text-lg font-medium mb-4 flex items-center gap-2">
          <span class="text-2xl">🏷️</span> Form Labels
        </h3>
        
        <div class="grid md:grid-cols-2 gap-4">
          <div class="bg-red-500/10 border border-red-500/30 rounded-lg p-4">
            <h4 class="text-red-400 font-mono text-sm mb-2">❌</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;input placeholder="Email"&gt;</code></pre>
            <p class="text-zinc-500 text-xs mt-2">Placeholder ist kein Label!</p>
          </div>
          <div class="bg-green-500/10 border border-green-500/30 rounded-lg p-4">
            <h4 class="text-green-400 font-mono text-sm mb-2">✅</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;label for="email"&gt;Email&lt;/label&gt;
&lt;input id="email"&gt;</code></pre>
          </div>
        </div>
      </article>
    </section>

    <!-- ARIA -->
    <section class="mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-zinc-200">
        ARIA Basics
        <span class="text-sm font-normal text-zinc-500 ml-2">Wenn HTML nicht reicht</span>
      </h2>

      <article class="bg-zinc-900/50 border border-zinc-800 rounded-xl p-6">
        <p class="text-zinc-400 text-sm mb-4">
          <strong>Erste Regel von ARIA:</strong> Benutze kein ARIA wenn du natives HTML nutzen kannst.
        </p>
        
        <div class="space-y-4">
          <div>
            <h4 class="text-blue-400 font-mono text-sm mb-2">aria-label</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;button aria-label="Menü schließen"&gt;✕&lt;/button&gt;</code></pre>
          </div>
          
          <div>
            <h4 class="text-blue-400 font-mono text-sm mb-2">aria-hidden</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;span aria-hidden="true"&gt;🎨&lt;/span&gt; Design</code></pre>
            <p class="text-zinc-500 text-xs mt-1">Dekorative Icons vor Screenreadern verstecken</p>
          </div>
          
          <div>
            <h4 class="text-blue-400 font-mono text-sm mb-2">aria-live</h4>
            <pre class="text-sm"><code class="text-zinc-300">&lt;div aria-live="polite"&gt;3 Ergebnisse gefunden&lt;/div&gt;</code></pre>
            <p class="text-zinc-500 text-xs mt-1">Dynamische Änderungen ankündigen</p>
          </div>
        </div>
      </article>
    </section>

    <!-- Testing -->
    <section class="mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-zinc-200">Testen</h2>
      
      <div class="grid md:grid-cols-2 gap-4">
        <div class="bg-zinc-900/50 border border-zinc-800 rounded-xl p-4">
          <h3 class="font-medium mb-2">🔧 Tools</h3>
          <ul class="space-y-1 text-sm text-zinc-400">
            <li>• Lighthouse (Chrome DevTools)</li>
            <li>• axe DevTools Extension</li>
            <li>• WAVE Extension</li>
          </ul>
        </div>
        <div class="bg-zinc-900/50 border border-zinc-800 rounded-xl p-4">
          <h3 class="font-medium mb-2">⌨️ Manuell</h3>
          <ul class="space-y-1 text-sm text-zinc-400">
            <li>• Nur mit Keyboard navigieren</li>
            <li>• Screenreader testen (VoiceOver)</li>
            <li>• Zoom auf 200% setzen</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Checklist -->
    <section class="mb-16 p-6 bg-green-500/10 border border-green-500/30 rounded-xl">
      <h3 class="text-green-400 font-medium mb-3">✅ Quick Checklist</h3>
      <div class="grid md:grid-cols-2 gap-2 text-sm text-zinc-300">
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Alle Bilder haben alt Text
        </label>
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Farbkontrast ≥ 4.5:1
        </label>
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Focus States sichtbar
        </label>
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Alle Forms haben Labels
        </label>
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Semantic HTML verwendet
        </label>
        <label class="flex items-center gap-2">
          <input type="checkbox" class="rounded" /> Keyboard-Navigation funktioniert
        </label>
      </div>
    </section>

    <nav class="flex gap-4 pt-8 border-t border-zinc-800">
      <a href="/performance" class="text-blue-400 hover:text-blue-300">← Performance</a>
      <a href="/" class="text-blue-400 hover:text-blue-300 ml-auto">Home →</a>
    </nav>

  </main>
</Layout>
